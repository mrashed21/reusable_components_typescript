// const decimals_number = /^(0*[1-9]\d*(\.\d+)?|0*\.\d+)$/;

// const password =
//   /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/;

// const message =
//   "Must contain 1 uppercase, 1 lowercase, 1 number, 1 special character";

// const password_validate = `
//  validate: {
//               minLength: (v: string) =>
//                 v.length >= 8 || "Password must be at least 8 characters",
//               hasUpper: (v: string) =>
//                 /[A-Z]/.test(v) || "Must contain at least 1 uppercase letter",
//               hasLower: (v: string) =>
//                 /[a-z]/.test(v) || "Must contain at least 1 lowercase letter",
//               hasNumber: (v: string) =>
//                 /\d/.test(v) || "Must contain at least 1 number",
//               hasSpecial: (v: string) =>
//                 /[@$!%*?&]/.test(v) ||
//                 "Must contain at least 1 special character",
//             },`;

// // dont need `` when use it.

// const text = /^[A-Za-z]+$/i;
